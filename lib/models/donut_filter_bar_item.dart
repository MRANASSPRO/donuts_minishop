class DonutFilterBarItem{
  int? id;
  String? label;

  /// a PODO model to know the label to display, and the id to uniquely identify the currently selected filter bar item
  DonutFilterBarItem({this.id, this.label});

}